0. move cache db to data folder maybe? or have an option to integrate something like external db
1. Replace saving as file with git or blob database whatever
2. more integrations
3. no-code scraper builder, maybe also a notebook like interface for low code
4. error handling, retry logic, also rework the queue system
5. more complex workflows
6. AI html scraping that detects schema
7. schema registry
8. an option to download the payloadas whatever
9. might break most of the stuff but, a variable registry where one can define maybe tokens or commonly used strings, and then use them in the scraper code or input fields everywhere
10. maybe a native storage system for the payloads to be stored and queried. DECIDE if it's worth/too much/ would be nice or makes chaotic
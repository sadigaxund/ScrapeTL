version: '3.8'

services:
  app:
    build: .
    container_name: scrapetl
    ports:
      - "8000:8000"
    volumes:
      - app-data:/app/data
    environment:
      - APP_TIMEZONE=UTC
      # Point DB to the persistent volume
      - DATABASE_URL=sqlite:////app/data/scraper_registry.db
    restart: unless-stopped

volumes:
  app-data:
